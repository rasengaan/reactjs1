import React from "react";

export const FontColorContext = React.createContext("white");

export const FontSizeContext = React.createContext("2rem");

export const BGColorContext = React.createContext("black");

// export const FontColorContext = React.createContext("white");
